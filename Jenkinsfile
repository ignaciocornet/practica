pipeline {
    agent any
    stages {
        stage('🛠️ Construir') {
            steps {
                echo 'Fase 1: Descargando código de GitHub...'
            }
        }
        stage('🧪 Probar') {
            steps {
                echo 'Fase 2: Ejecutando pruebas mágicas...'
            }
        }
    }
}
